# Contrato del proyecto

> Fuente de verdad única, leída tanto por Claude Code como por Aider
> (ver `.aider.conf.yml` → `read: CLAUDE.md`). Completar por proyecto:
> cuanto más preciso, menos contexto tiene que adivinar el agente y menos alucina.
> Si ya existía un CLAUDE.md en este proyecto (generado con `/init` u otro), fusionar en vez de pisar.

## Stack
- Lenguaje / framework:
- Gestor de paquetes / build:
- Versión mínima runtime:

## Comandos
- Instalar deps:
- Dev / run local:
- Test:
- Lint / typecheck:
- Build:

## Arquitectura (resumen de 3-5 líneas)
-

## Convenciones de código
- Estilo / linter:
- Naming (archivos, funciones, componentes):
- Estructura de carpetas (qué va dónde):

## Testing
- Cómo se corren los tests:
- Qué cubrir obligatoriamente antes de aceptar un cambio:

## No tocar / cuidado
- Carpetas o archivos legacy, generados, o de terceros que el agente NO debe editar:
- Invariantes no obvias (ej. "este archivo se genera automáticamente", "esto lo consume otro servicio y el contrato no se puede romper"):

## Notas para el agente
- Preferencias de commits (mensaje, granularidad):
- Instrucciones persistentes (ej. "siempre correr el lint antes de terminar"):
