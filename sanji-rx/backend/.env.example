# sanji-rx NO necesita .env propio.
# Las variables se leen del .env compartido en:
#   teoria-sintergica/brain-prototype/backend/.env
#
# Variables necesarias (ya están en ese archivo):
#   SANJI_POSTGRES_HOST / PORT / DB / USER / PASSWORD
#   CLAUDE_API_KEY  (usado como ANTHROPIC_API_KEY)
